<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:id="@+id/fragment_splash_main_container"

    style="@style/Container.Green"

    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- CONTENT -->
    <LinearLayout
        android:id="@+id/fragment_splash_inner_container"

        android:layout_width="0dp"
        android:layout_height="wrap_content"

        android:orientation="vertical"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        >

        <!-- ICON -->
        <ImageView
            android:id="@+id/fragment_splash_icon"

            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:src="@drawable/tennis_ball"
            />

        <!-- TEXT -->
        <com.begoandapps.padelapp.components.AppTextView
            android:id="@+id/fragment_splash_text"

            style="@style/BaseText.Title.Black"

            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:text="@string/app_sport"
            />

    </LinearLayout>

</android.support.constraint.ConstraintLayout>
